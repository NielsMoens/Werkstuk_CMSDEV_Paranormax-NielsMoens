{% extends "_layouts/site" %}
{#
    Contact page form rendered using the freeForm plugin
#}

{% block content %}
    <section class="site-hero site-hero-innerpage overlay" data-stellar-background-ratio="0.5" style="background-image: url();">
        <div class="container">
            <div class="row align-items-center site-hero-inner justify-content-center">
                <div class="col-md-8 text-center">
                    <div class="mb-5 element-animate">
                        <h1>Contact Us</h1>
                    </div>
                </div>
            </div>
        </div>
    </section>
    {{ craft.freeform.form("contactForm").render() }}
{% endblock %}






